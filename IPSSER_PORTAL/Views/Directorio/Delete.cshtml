@model IPSSER.DATOS.TBL_PORTAL_DIRECTORIO_INTERNO

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>TBL_PORTAL_DIRECTORIO_INTERNO</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Departamento
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Departamento)
        </dd>
        <dt>
            Extensión
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Extension)
        </dd>
        <dt>
            Email
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Email)
        </dd>
    </dl>

    @using (Html.BeginForm("Delete", "Directorio", FormMethod.Post, new { onsubmit = "if (!confirm('¿Confirma eliminar este registro?')){ return false; }" }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Eliminar" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
