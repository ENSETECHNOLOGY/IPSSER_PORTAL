@model IEnumerable<IPSSER.DATOS.TBL_PORTAL_CONTENIDO_IMAGENES>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TBL_PORTAL_BIBLIOTECA_IMAGENES.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TBL_PORTAL_CONTENIDOFRONT.Titulo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TBL_PORTAL_BIBLIOTECA_IMAGENES.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TBL_PORTAL_CONTENIDOFRONT.Titulo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdContenidoImagen }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdContenidoImagen }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdContenidoImagen })
        </td>
    </tr>
}

</table>
