@model IEnumerable<IPSSER.DATOS.TBL_PORTAL_FRONT>

@{
    ViewBag.Title = "Index";
}

<br />
<center>
    <h2>DEFINICION DE FRONT</h2>
</center>
<br />
<br />

<p>
    @Html.ActionLink("CREAR NUEVO FRONT", "Create", null, new { @class = "btn btn-primary btn-xs" })
</p>

<div class="table-responsive">
    <table class="table table-bordered table-hover">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Titulo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TBL_PORTAL_MODULO.NombreModulo)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Titulo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TBL_PORTAL_MODULO.NombreModulo)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.IdFront }, new { @class = "btn btn-primary btn-xs" }) |
                    @Html.ActionLink("Ver Detalle", "Details", new { id = item.IdFront }, new { @class = "btn btn-primary btn-xs" }) |
                    @Html.ActionLink("Borrar", "Delete", new { id = item.IdFront }, new { @class = "btn btn-primary btn-xs" })
                </td>
            </tr>
        }

    </table>
</div>
