@model IPSSER.DATOS.TBL_PORTAL_DOCUMENTO

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>TBL_PORTAL_DOCUMENTO</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Titulo
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Titulo)
        </dd>

        <dt>
            Archivo
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Archivo)
        </dd>
    </dl>

    @using (Html.BeginForm("Delete", "Documentos", FormMethod.Post, new { onsubmit = "if (!confirm('¿Confirma eliminar este registro?')){ return false; }" }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Eliminar" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
